ruff==0.16.1
pytest-homeassistant-custom-component==0.13.355
pytest-cov==7.1.0
yamllint==1.38.0
check-jsonschema==0.38.0
codespell==2.4.3
# CI installs this file; the devcontainer installs the pip list in
# workbench/docker/Dockerfile. Pin in both or it is missing from one.
#
# mutmut is out of version_matrix.json by design — no HA counterpart, no
# pre-commit hook, no CI use.
mutmut==3.7.0
