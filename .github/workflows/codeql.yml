---
name: CodeQL

"on":
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 6 * * 1" # Weekly Monday 06:00 UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  codeql:
    permissions:
      contents: read
      security-events: write # Required to upload CodeQL SARIF results to GitHub Security
    # Branch @main — replace SHA after releasing .github repo with codeql.yaml v2.0.14
    uses: PlayFaster/.github/.github/workflows/codeql.yaml@b12898ea9b0d954deaf2a099f6bb9fb1fd0bc041
