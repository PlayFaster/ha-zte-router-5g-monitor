---
name: CodeQL

"on":
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 6 * * 1" # Weekly Monday 06:00 UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  codeql:
    permissions:
      contents: read
      security-events: write # Required to upload CodeQL SARIF results to GitHub Security
    # Branch @main — replace SHA after releasing .github repo with codeql.yaml v2.0.5
    uses: PlayFaster/.github/.github/workflows/codeql.yaml@e604ffdf9041f085834e635faecd428c78fe8d6a
