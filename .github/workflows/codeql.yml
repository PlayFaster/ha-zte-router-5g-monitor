---
name: CodeQL

"on":
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 6 * * 1" # Weekly Monday 06:00 UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  codeql:
    permissions:
      contents: read
      security-events: write # Required to upload CodeQL SARIF results to GitHub Security
    # Branch @main — replace SHA after releasing .github repo with codeql.yaml
    uses: PlayFaster/.github/.github/workflows/codeql.yaml@553c5646db65c5c93d325aa1cb32fc91223c9c4e
